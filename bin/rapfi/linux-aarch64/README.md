# Raspberry Pi Rapfi

Place the Raspberry Pi / Linux ARM64 Rapfi executable here:

```text
bin/rapfi/linux-aarch64/rapfi
```

After copying it in, make sure it is executable:

```bash
chmod +x bin/rapfi/linux-aarch64/rapfi
```
